environment:
  matrix:
    - PYTHON: "C:\\Python36"
    - PYTHON: "C:\\Python36-x64"

install:
  - "%PYTHON%\\python.exe -m pip install tox cx_Freeze"

build: off

test_script:
  - "build.cmd %PYTHON%\\python.exe -m tox"

after_test:
  - "build.cmd %PYTHON%\\python.exe setup.py bdist_msi"

artifacts:
  - path: dist\*
