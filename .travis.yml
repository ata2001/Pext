language: cpp

install:
    - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
    - sudo apt-get update -qq
    - sudo apt-get install -qq qt5-qmake python3-pip python3-pyqt5
    - export QMAKE=/usr/lib/x86_64-linux-gnu/qt5/bin/qmake
    - python3 -m pip install --user dulwich pyqtdeploy

script:
    - python3 --version
    - pyqtdeploy-build --sysroot /usr pext.pdy
    - cd build-*
    - qmake -qt=5
    - make
